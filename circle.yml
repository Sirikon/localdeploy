dependencies:
  override:
    - go get -u github.com/jstemmer/go-junit-report
test:
  override:
    - "go test -v | go-junit-report > report.xml":
      files:
        - report.xml
general:
  artifacts:
    - "molly" # Executable
# deployment:
#   master:
#     branch: master
#     commands:
#       - ./deploy.sh